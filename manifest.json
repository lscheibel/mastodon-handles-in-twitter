{
  "name": "Mastodon Handles in Twitter",
  "description": "Show the Mastodon handle of a Twitter user right next to their tweets. This works similarly to \"Debirdify\" by searching through the users bio.",
  "version": "1.0.0",
  "manifest_version": 3,
  "permissions": ["https://*.twitter.com/*", "scripting"],
  "host_permissions": ["https://*.twitter.com/*"],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["https://*.twitter.com/*"],
      "css": ["styles.css"]
    }
  ],
  "action": {
    "default_icon": "icon.png"
  }
}
